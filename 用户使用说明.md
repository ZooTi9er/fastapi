# FastAPI MCP SSE 服务使用说明

## 概述

本项目是一个基于 FastAPI 和 Model Context Protocol (MCP) 的 Server-Sent Events (SSE) 服务，为 AI 代理提供了标准化的工具调用接口。

**服务地址**: http://mini.ewuzhe.dpdns.org:1234

## 快速开始

### 前提条件

- Python 3.12+
- 支持 SSE 的 HTTP 客户端
- 基本的 REST API 调用知识

### 服务访问

服务已部署并运行在公网，您可以通过以下地址访问：

```
http://mini.ewuzhe.dpdns.org:1234
```

## API 端点

### 1. 基础端点

#### 根路径 - Hello World
```
GET /
```
**返回**:
```json
{
    "message": "Hello World"
}
```

#### 健康检查
```
GET /health
```
**返回**:
```json
{
    "status": "healthy",
    "timestamp": "2025-09-17T16:47:54.611699",
    "service": "fastapi-mcp-sse-server"
}
```

#### 用户问候
```
GET /hello/{name}
```
**参数**:
- `name` (路径参数): 用户名称

**返回**:
```json
{
    "message": "Hello, {name}!"
}
```

**示例**:
```
GET /hello/testuser
```
```json
{
    "message": "Hello, testuser!"
}
```

### 2. MCP SSE 端点

#### MCP SSE 连接
```
GET /sse
```
此端点提供 Server-Sent Events 连接，用于 AI 代理与 MCP 服务的实时通信。

**功能**:
- 自动生成会话 ID
- 支持 MCP 协议消息传输
- 实时双向通信

#### MCP 消息端点
```
POST /sse/messages/?session_id={session_id}
```
用于发送 MCP 消息到指定会话。

## MCP 工具集成

### 可用工具

通过 MCP 协议，以下工具可供 AI 代理调用：

1. **say_hello** (operation_id="say_hello")
   - 路径: `GET /`
   - 功能: 返回简单的 Hello World 消息
   - 参数: 无

2. **health_check** (operation_id="health_check")
   - 路径: `GET /health`
   - 功能: 检查服务健康状态
   - 返回: 状态、时间戳和服务信息

3. **greet_user** (operation_id="greet_user")
   - 路径: `GET /hello/{name}`
   - 功能: 向指定用户发送问候
   - 参数: `name` (字符串)

## 使用示例

### 基础 API 调用

```bash
# 获取 Hello World
curl http://mini.ewuzhe.dpdns.org:1234/

# 检查健康状态
curl http://mini.ewuzhe.dpdns.org:1234/health

# 向用户问好
curl http://mini.ewuzhe.dpdns.org:1234/hello/张三
```

### MCP SSE 连接测试

```bash
# 测试 SSE 连接 (会持续监听事件)
curl -N http://mini.ewuzhe.dpdns.org:1234/sse
```

### Python 客户端示例

```python
import requests
import sseclient
import json

# 基础 API 调用
def test_basic_endpoints():
    # Hello World
    response = requests.get("http://mini.ewuzhe.dpdns.org:1234/")
    print("Hello World:", response.json())

    # 健康检查
    response = requests.get("http://mini.ewuzhe.dpdns.org:1234/health")
    print("Health Check:", response.json())

    # 用户问候
    response = requests.get("http://mini.ewuzhe.dpdns.org:1234/hello/世界")
    print("Greeting:", response.json())

# MCP SSE 连接
def test_mcp_sse():
    messages_url = "http://mini.ewuzhe.dpdns.org:1234/sse"

    # 建立 SSE 连接
    response = requests.get(messages_url, stream=True)
    client = sseclient.SSEClient(response)

    for event in client.events():
        print(f"Event: {event.event}")
        print(f"Data: {event.data}")
        print("---")

if __name__ == "__main__":
    print("=== 测试基础端点 ===")
    test_basic_endpoints()

    print("\n=== 测试 MCP SSE 连接 ===")
    test_mcp_sse()
```

## 开发和部署

### 本地开发

1. **克隆项目**
```bash
git clone <repository-url>
cd fastapi
```

2. **创建虚拟环境**
```bash
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# 或 .venv\Scripts\activate  # Windows
```

3. **安装依赖**
```bash
pip install -r requirements.txt
```

4. **运行服务**
```bash
python main.py
```

服务将在 `http://localhost:1234` 运行。

### 依赖管理

项目依赖在 `requirements.txt` 中定义：
```
fastapi==0.115.6
uvicorn[standard]==0.32.1
fastapi-mcp
```

### 服务配置

主要配置参数：
- **主机**: `::` (IPv6 兼容)
- **端口**: `1234`
- **重载**: `True` (开发模式)
- **日志级别**: `info`

## 故障排除

### 常见问题

1. **连接超时**
   - 检查网络连接
   - 确认服务地址正确
   - 防火墙设置

2. **MCP 连接失败**
   - 确认客户端支持 SSE
   - 检查会话 ID 是否正确
   - 验证消息格式

3. **服务不可用**
   - 检查服务状态
   - 查看日志输出
   - 重启服务

### 日志检查

服务日志会输出到控制台和 `logs/app.log` 文件。

## 技术架构

### 核心组件

1. **FastAPI**: Web 框架，提供 REST API
2. **fastapi-mcp**: MCP 协议集成库
3. **Uvicorn**: ASGI 服务器
4. **Server-Sent Events**: 实时通信协议

### 架构图

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   AI Agent     │    │   MCP Client    │    │   Web Client    │
└─────────┬───────┘    └─────────┬───────┘    └─────────┬───────┘
          │                      │                      │
          └──────────────────────┼──────────────────────┘
                                 │
                    ┌─────────────────┐
                    │   FastAPI       │
                    │   MCP SSE       │
                    │   Server        │
                    └─────────┬───────┘
                              │
               ┌─────────────────────────────┐
               │       Available Tools       │
               │  • say_hello                │
               │  • health_check             │
               │  • greet_user               │
               └─────────────────────────────┘
```

## 安全考虑

- 服务目前运行在开发模式
- 建议在生产环境中添加认证机制
- 考虑使用 HTTPS 加密通信
- 实施速率限制以防止滥用

## 监控和维护

### 健康检查

定期检查 `/health` 端点确保服务正常运行：

```bash
curl http://mini.ewuzhe.dpdns.org:1234/health
```

### 性能监控

- 监控响应时间
- 跟踪并发连接数
- 观察资源使用情况

## 更新日志

### v1.0.0 (2025-09-17)
- 初始版本发布
- 实现 MCP SSE 服务
- 提供基础 API 端点
- 支持公网访问

## 支持和反馈

如有问题或建议，请通过以下方式联系：
- 创建 GitHub Issue
- 发送邮件至项目维护者
- 查看项目文档和示例代码

## 许可证

项目许可证信息请查看项目根目录的 LICENSE 文件。

---

**注意**: 本服务主要用于演示和测试目的。在生产环境中使用前，请确保实施适当的安全措施和性能优化。